<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@null"
    android:fitsSystemWindows="true">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/detail_app_bar_layout_height"
        android:fitsSystemWindows="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <android.support.design.widget.CollapsingToolbarLayout
            android:id="@+id/detail.collapsing_toolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:fitsSystemWindows="true"
            app:contentScrim="?attr/colorPrimary"
            app:expandedTitleMarginEnd="@dimen/expanded_title_margin_end"
            app:expandedTitleMarginStart="@dimen/expanded_title_margin_start"
            app:layout_scrollFlags="scroll|exitUntilCollapsed">

            <android.support.v7.widget.Toolbar
                android:id="@+id/detail.toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@null"
                app:layout_collapseMode="pin"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

            <TableLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal|center_vertical"
                android:background="@null">

                <TableRow style="@style/DeviceTableRaw">

                    <TextView
                        style="@style/DeviceLabel"
                        android:text="@string/detail.device.label.name"/>

                    <TextView
                        android:id="@+id/detail.name"
                        style="@style/DeviceLabel.DeviceText"/>
                </TableRow>

                <TableRow style="@style/DeviceTableRaw">

                    <TextView
                        style="@style/DeviceLabel"
                        android:text="@string/detail.device.label.rssi"/>

                    <TextView
                        android:id="@+id/detail.rssi"
                        style="@style/DeviceLabel.DeviceText"/>
                </TableRow>

                <TableRow style="@style/DeviceTableRaw">

                    <TextView
                        style="@style/DeviceLabel"
                        android:text="@string/detail.device.label.address"/>

                    <TextView
                        android:id="@+id/detail.address"
                        style="@style/DeviceLabel.DeviceText"/>
                </TableRow>

                <TableRow style="@style/DeviceTableRaw">

                    <TextView
                        style="@style/DeviceLabel"
                        android:text="@string/detail.device.label.state"/>

                    <TextView
                        android:id="@+id/detail.state"
                        style="@style/DeviceLabel.DeviceText"/>
                </TableRow>
            </TableLayout>
        </android.support.design.widget.CollapsingToolbarLayout>
    </android.support.design.widget.AppBarLayout>

    <FrameLayout
        android:id="@+id/detail.fragment_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@null"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

    </FrameLayout>
</android.support.design.widget.CoordinatorLayout>